

let placements = [1, 2, 3, 4, 5, 6, 7, 8, 9]


enum Player {
    
    case one
    case two
}

enum Space {
    case empty
    case x
    case o
}

var currentPlayer = Player.one


func boardPlacement(currrentSpace: Space, player: Player) -> Bool {
    var currentSpace = Space.empty
    switch currentSpace {
        
    }
    
}
